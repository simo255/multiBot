# Contributing

1. Fork → branch → PR
2. Test on a Grok Bot box: `./install.sh`, import multiBot, run `/create-bot`
3. Keep skills under `workflows/`; child rules under `templates/`

Ideas: email/calendar template packs, MCP bootstrap snippets, non-interactive create script.
